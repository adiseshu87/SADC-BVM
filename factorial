
#include <stdio.h>
int fact(int );
int main()
{
    int f,n;
    printf("enter  n ");
    scanf("%d",&n);
    f=fact(n);
    
    printf("%d factorial is %d ",n,f);

    return 0;
}

int fact(int n)
{
    if(n==0)
    return 1;
    else
    return n*fact(n-1);

}
